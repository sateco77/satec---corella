services:
  - type: worker
    name: render
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    port: 8080 
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
